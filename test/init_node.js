// we just do this so we can use the same tests for
// the browser version and the node version.
var Rune = {}
Rune.Font = require("../tmp/font.node.js");

// specs.js will automatically be inserted here.