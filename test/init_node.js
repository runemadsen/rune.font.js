// We require Rune so we can use the same tests as
// the browser version.
var Rune = require("rune.js");
Rune.Font = require("../src/font.js");
var _ = require("underscore");

// specs.js will automatically be inserted here.
